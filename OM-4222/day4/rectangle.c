#include<stdio.h>
int main(){
	int L,B,area;
	printf("enter the value of L:");
	scanf("%d",&L);
	printf("enter the value of B:");
	scanf("%d",&B);
	area = L*B;
	printf("area of rectangle is : %d",area);
	return 0;
}